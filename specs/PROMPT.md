Regola valida anche per le nuove versioni: ogni volta che fai un cambio funzionale oppure correggi un bug, valuta se ha senso inserirlo nei test automatici e, in questo caso, aggiorna i test "giusti" (smoke, regressione, end2end etc).  

Quando si apre la sidebar, dammi un modo per chiuderla senza selezionare una voce di menu.
Bug: Il pulsante "registra correzione" nella pagina "referto" ed i tasti per la traduzione in più lingue sembrano agire solo sui campi S/O/A/P. Verifica che agiscano anche su "Dati clinici specialistici" e su "Checklist (template)"
In archivio inserisci in fondo una piccola nota, in cui dici che tenendo premuto il referto appare una "x" che permette di cancellarlo.
Quando premo la registrazione nella pagina visita, appaiono informazioni di dettaglio, come ad esempio "Chunkin ON", "Profilo: andorid" etc. Queste informazioni non devono apparire se il debug è disattivato.
Bug: quando attivo la registrazione e la completo, appare subito il messaggio "Trascrizione completa - puoi generare il referto" anche se la trascrizione è ancora in corso. Quinid: il messaggio che deve apparire è qualcosa come "Sto trascrivendo la registrazione.". Quando termina la trascrizione il messaggio cambia in: "Ho completato la trascrizione la registrazione. Sto generando il referto" ed avvia subito la generazione del referto, senza attendere la pressione del tasto da parte dell'utente. Quando la generazione del referto è completata, messaggio cambia in: "Ho completato la generazione del referto" e (come fa già adesso) apre immediatamente la pagina referto.
A volte, quando crea il referto, ripete le stesse informazioni in uno dei campi S/O/A/P e poi nei campi "Dati clinici specialistici". Non va bene: se una certa informazione c'è nei campi S/O/A/P allora non deve essere ripetuta dopo.  
In tutti i posti in cui c'è "Diario clinico", sostituisci con "Profilo sanitario". Quando lo generi, invece di "[Nome del Veterinario]" usa il Nome veterinario registrato in Impostazioni ed al posto di "[Data]" usa la data in cui è stato generato il testo.
Bug: Quando seguo il flusso "Domande & Risposte" -> "Domande su un referto" -> Seleziono referto -> "Apri / Genera spiegazione", a volte si apre la spiegazione "Per il proprietario" di un altro referto.
Nella pagina "Domande & Risposte" -> "Domande sul tuo pet" applica le API di moderazione sia sulle domande sia sulle risposte. L'utente può fare domande non solo sul suo pet, ma su qualunque argomento che ha a che fare con i pet e gli animali in generale. Non può fare domande che esulano da questo contesto.
Nella pagina "Domande & Risposte" -> "Tips & Trick" il pulsante "Leggi" deve essere inattivo e grigiato se non c'è nessun tip da leggere. 
Bug: se premo un pulsante per la traduzione, ad es. EN, il pulsante deve rimanere attivo solo sul documento che è stato tradotto. Se cambio pagina o apro un nuovo documento, il pulsante attivo è quello congruente con pagina/documento.
La sezione "API Key" passa dalla pagina "Impostazioni" a "Debug". In "Impostazioni" crea un gruppo "Informazioni clinica" che contiene "Logo clinica" e "Nome veterinario". Il gruppo "Sistema", perciò rimarra solo con il flag per il debug e le informazioni sulla versione. La sezione "Logo clinica" si può chiudere ed aprire.
